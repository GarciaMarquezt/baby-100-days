server:
  port: 8080

spring:
  application:
    name: baby100-api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://8.138.140.178:3306/baby100_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: baby100
    password: Ypc@970612
  
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

app:
  cors:
    allowed-origins: "*"
    allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"
  upload:
    # 上传文件保存路径（服务器路径，已迁移到网站根目录）
    dir: /www/wwwroot/baby-web/uploads/
    # 文件访问域名（使用主域名避免SSL证书问题）
    domain: https://1101020.xyz/uploads/
    # SFTP配置（用于远程上传）
    sftp:
      enabled: true
      host: 8.138.140.178
      port: 22
      username: root
      password: Ypc@970612
      # 或者使用私钥路径
      # privateKey: ~/.ssh/id_rsa
